services:
  bilibilitopic:
    build: .
    container_name: bilibilitopic
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
      BASE_PATH: /btopic
